skills_dir: "skills/characters"
templates_dir: "skills/templates"
default_max_turns: 10

scheduler:
  urgency_weight: 1.0
  tension_weight: 1.0
  conflict_weight: 1.2
  consecutive_penalty: 0.7

director:
  agent_id: "director"
  provider: ""
  model: ""
  temperature: 0.2
  max_retries: 1

memory:
  top_k: 5
  recency_decay: 0.3

action:
  temperature: 0.7
  max_tokens: 700
  max_retries: 2

auto_character:
  enabled: true
  trigger_on_profile_save: true
  trigger_on_scene_start: true
  book_skills_root: "skills/books"
  overwrite_existing: false
  max_new_roles_per_run:
  provider:
  model:
  temperature: 0.4
  max_tokens: 1800
  agent_id: "auto_character"

character_state:
  enabled: true
  auto_apply_confidence: 0.75
  max_updates_per_turn: 8
  persist_change_events: true
